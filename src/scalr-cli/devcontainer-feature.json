
{
    "name": "Scalr CLI",
    "id": "scalr-cli",
    "version": "1.0.0",
    "description": "scalr is a command-line tool that communicates directly with the Scalr API",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "0.16.2"
            ],
            "default": "0.16.2",
            "description": "Select the version of scalr cli"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
